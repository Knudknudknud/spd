\documentclass{article}
\usepackage{arxiv} 
\usepackage{graphicx} % Inserting images
\graphicspath{{Images/}} % Expects Image folder in current folder.
\usepackage{float} % Improves h specifier
\usepackage{booktabs}
\usepackage{tabularx}



\usepackage[style=alphabetic]{biblatex}
\addbibresource{references.bib}

\usepackage{amsmath}
\usepackage{amssymb}


\begin{document}
\title{Multidimensional Parameter Decomposition for Mechanistic Interpretability}
\author{Knud Rasmus Sønderbek}
\maketitle
\begin{abstract}
Some super cool abstract of all my findings \footnote{Very important to add quantization somewhere. https://arxiv.org/pdf/2303.13506 also paper on clustering in spd. Perhaps manifold clustering stearng too.}
\end{abstract}

\tableofcontents


\section{Introduction}

Mechanistic interpretability seeks to reverse-engineer neural
networks into a set of human-interpretable mechanisms.
Different branches of the field pursue this goal with different focal points: Either through directions in activation space, computational subgraphs, or components of the network's parameters.

However, both the atomic structure for decomposition
 and the decomposition itself remain open problems.
\autocite{sharkey2025openproblemsmechanisticinterpretability}
Whether a decomposition recovers the computational primitives
of a neural network depends on what the ground-truth decomposition
is taken to be. With models consisting of independent features,
the decomposition is unambiguous, with one atom per feature.
If the features are dependent or co-occur, the decomposition becomes underdetermined.
Decompositions that split features across multiple atoms, risk losing the structural information, that gives each feature its meaning,
while decompositions that group multiple features into a single atom, risk losing granularity by collapsing distinct features into a single unit.

For example, a network computing the volume of a cube can be
decomposed into components corresponding to individual side
lengths, or into a single component computing the volume directly.
The former corresponds to a feature-level decomposition,
with one atom per input direction, exposing internal
structure but leaving grouping implicit, only recoverable
through co-activation patterns. The latter corresponds
to a structural-level decomposition, with one atom per
computation, exposing the grouping directly but collapsing per side-length information.

Recently, 
a formal definition of irreducibility was introduced to disambiguate cases such
as the cube example. \footnote{I got this deifnition wrong, we decompose f not the V's, we thus musn't show delta orthogonolaity,
and the argument with that the V's can be linear combinations isnt as important anymore. However we still have the same argument, that
they can be low dimensional instead of rank 1. Secondly we need to state clearly that f is a function $f: d_{input} \to  \mathbb{R}^{d_{fi}}$}
\autocite{engels2025languagemodelfeaturesonedimensionally}
Features are irreducible, and thus constitute units of decomposition,
if their joint distribution cannot be decomposed into independent
factors or into a mixture containing a lower-dimensional component.

In terms of the decomposition itself,
recent work focuses on decomposing parameters, as opposed the activations.
These methods aim to recover components that are faithful to the original weights, sparse per input, and individually simple. 
One such approach, Stochastic Parameter Decomposition (SPD) \autocite{stochasticparameterdecomposition}, factorizes networks into rank-one components with a learned per component importance
function.

This work extends SPD by examining its behaviour on dependent features,
in settings where the multi-dimensional feature hypothesis posits
a feature-level or a structural-level unit \footnote{I mean feature-level, structure level is wrong under this view but it sounds good} \autocite{engels2025languagemodelfeaturesonedimensionally}. 
Additionally a reformulation of the importance function is proposed,
to encourage that features that are manipulated jointly are grouped together, by computing importance relationally across components.


My findings
% The findings in this work suggest that while SPD does perform better in terms of faithfulness, the method struggles in terms of interpretability,
% where the importance values do not 
% While this extension is theoretically well grounded, it only manages to partially recover the ground-truth decompositions,
% while it correctly identifies the correct direction of the features, it comes at a cost of feature shrinking, which is not observed in SPD.
% However in terms of interpretability


\section{Related work}
\subsection{The linear representation hypothesis}
A widespread view in mechanistic interpretability is the linear representation hypothesis: high-level concepts are encoded as directions in a network's representation space \autocite{park2024linearrepresentationhypothesisgeometry}. Together with the superposition hypothesis \autocite{elhage2022toymodelssuperposition}, it forms a central tenet of interpretability: each feature corresponds to a single direction, and a hidden state is a sparse linear combination of feature directions.

Recent work questions how strictly this view should be taken, distinguishing between a weak form of the hypothesis, that some features are linear, from a stronger form that all features are \autocite{mendel2024sae,smith2024strong}. 
Empirically multiple counterexamples to the stronger form have been proposed, include recurrent networks encoding token position by magnitude rather than direction \autocite{csordás2024recurrentneuralnetworkslearn}, transformers representing cyclical concepts such as days of the week \autocite{engels2025languagemodelfeaturesonedimensionally} or modular arithmetic \autocite{nanda2023progressmeasuresgrokkingmechanistic} in irreducible low-dimensional subspaces.
\subsection{Activation-space decomposition}
Decomposing a network's activations predominantly focuses on disentangling layer activations into interpretable features.
Here, sparse autoencoders (SAE) are one of the most widely researched methods \autocite{sparseautoencodershighlyinterpretable, bricken2023monosemanticity},
that learn an overcomplete dictionary that expresses each hidden state as a sparse combination of one-dimensional features.
However, these features are not necessarily aligned with the network's computation, due to feature absorption, inconsistencies across dictionary sizes, and a failure to capture feature geometry.
 \autocite{absorptionstudyingfeaturesplitting, sparseautoencoderscanonicalunits,mendel2024sae}.

Circuit discovery extends feature analysis to identify
how a computation is processed by the network's structure,
by identifying  minimal subgraphs required to perform a given task,
without loss of performance. \autocite{olah2020zoomin,marks2025sparsefeaturecircuitsdiscovering}.
However, these methods generally do not account for the
polysemantic and densely connected structure of neural networks,
where the functional behaviour can be attributed to multiple distinct but functionally equivalent subgraphs \autocite{méloux2025everythingeverywhereoncemechanistic}.

\subsection{Parameter-space decomposition}

Recently a newer line of work aims to decompose the network's parameters directly, instead of the activations.
Transcoders \autocite{dunefsky2024transcodersinterpretablellmfeature} represent an intermediate step that learns a sparse approximation of an MLP layer. Parameter decomposition methods go further, factorising the network's weights directly, with the goal of recovering the network's computational primitives.

Most directly relevant to this work is the description-length-driven
line of Attribution-based Parameter Decomposition
\autocite{AttributionBasedParameterDecomposition} and
Stochastic Parameter Decomposition (SPD)
\autocite{stochasticparameterdecomposition}, which has
recently been explored in small transformer models
\autocite{christensen2025decompositionsmalltransformermodels}
and cross-layer clustering \autocite{seshadri2026crosslayer}.
While APD decomposes a network into multidimensional
components,
the method is sensitive to hyperparameters, and does not scale beyond toy settings due
to computational complexity. SPD addresses this
by decomposing each weight matrix into rank-one
subcomponents and learning a per-input importance
function over them, at the cost of requiring post-hoc
clustering to recover higher-rank mechanisms;
both methods are detailed in
Section~\ref{sec:spd_background}. Other parameter-space
approaches include local loss landscape decomposition \autocite{chrisman2025identifyingsparselyactivecircuits}.



\section{Method}

\subsection{Background on SPD}
\label{sec:spd_background}
Attribution-based Parameter Decomposition (APD) \autocite{AttributionBasedParameterDecomposition}
and Stochastic Parameter Decomposition (SPD) \autocite{stochasticparameterdecomposition} both aim to decompose
a network's parameters into a set of components that are faithful
to the original weights, minimal in the number required per input,
and individually as simple as possible.

To formalise this, consider a neural network $f(x, W)$ with weight matrices $W = \{W^1, \dots, W^L\}$.
A set of parameter components $\{W^l_c\}_{c=1}^{C}$ are said to comprise the network's mechanisms if they satisfy the following properties:
\renewcommand\labelitemi{\tiny$\bullet$}
\begin{itemize}
    \setlength\itemsep{0em}
    \item \textbf{Faithfulness}: The parameter components should sum to the 
          parameters of the original network.
    \item \textbf{Minimality}: As few parameter components as possible should 
          be required to replicate the network's output on any given input.
    \item \textbf{Simplicity}: Each component should span as few weight 
          matrices and as few ranks as possible.
\end{itemize}

APD \autocite{AttributionBasedParameterDecomposition} approaches this by decomposing
the network into a set of parameter components without constraining
their rank, using a gradient-based attribution that selects the top-$k$ components most
responsible for any given input. While effective on small models,
APD scales poorly: its memory cost grows with the number of components,
as each component has the same dimensionality as the original network,
and its top-$k$ attribution is sensitive to hyperparameter choice.

SPD \autocite{stochasticparameterdecomposition} addresses these limitations by enforcing a rank-one structure on each component, allowing more components to be learned at fixed memory cost, and replacing top-$k$ attribution with a learned causal importance function. Formally, each weight matrix $W^l$ is decomposed into a sum of $C$ rank-one subcomponents:
\begin{equation}
W^{l}_{i,j} \approx \sum_{c=1}^{C} U^{l}_{c,i} V^{l}_{c,j}, 
\quad \text{where} \quad U^l \in \mathbb{R}^{C \times d_{\text{out}}}, \, V^l \in \mathbb{R}^{C \times d_{\text{in}}}
\end{equation}
where C is a hyperparameter, for the number of components for each layer, $l$ indexes the layer and $i,j$ are the hidden indices.
The subcomponent count, C, may be chosen to be larger than both $d_{in}$ and $d_{out}$, enabling the method to learn features in superposition \autocite{stochasticparameterdecomposition}.

To enforce the properties above, three losses are introduced, which are optimised jointly.
Faithfulness is enforced thorugh a mean squared error loss between the original network's weights and the the sum of subcomponents:

\begin{equation}
    \mathcal{L}_{\text{faithfulness}} = \frac{1}{N} \sum_{l=1}^{L}\sum_{i,j} \left( W_{i,j}^{l} - \sum_{c=1}^{C} U^{l}_{i,c}V^{l}_{c,j} \right)^{2}  
\end{equation}
where $N$ is the total parameter count in the target model.

Optimization for simplicity and minimality is achieved through a
stochastic masking procedure. Intuitively, if a subcomponent is not
necessary for reconstruction, it can be sampled to zero without loss
of faithfulness. A causal importance function
$\Gamma: \mathcal{X} \to [0,1]^{C \times L}$ is trained to learn the
lower bound of the sampling distribution. In SPD, $\Gamma$ is a set
of independent MLPs $\gamma^l_c$ that each take a layer's activation
and output a scalar importance for the corresponding subcomponent:
\begin{equation}
g^{l}_{c}(x) = \Gamma^l_c(x) = \sigma_{H}(\gamma^{l}_{c}(h^{l}_{c}(x))), \qquad h^{l}_{c}(x) = \vec{V}_{c}^{l\top} \vec{a}^l(x)
\end{equation}
where $\sigma_{H}$ is a hard sigmoid and $\vec{a}^l(x)$ is the
activation at layer $l$. These importance values serve as the lower bound for how much a subcomponent can be masked. With $r^l_{c} \sim \mathcal{U}(0, 1)$ sampled per subcomponent, the masked weight matrix is given by:
\begin{equation}
\begin{aligned}
    m_{c}^l(x, r) &= g^{l}_{c}(x) + \bigl(1 - g^{l}_{c}(x)\bigr)\, r^l_{c}, \\
    W'^{l}(x, r)  &= \sum_{c=1}^{C} \vec{U}_{i,c}^{l}\, m_{c}^l(x, r)\, \vec{V}_{c,j}^{l},
\end{aligned}
\end{equation}
These masked matrices are trained to reconstruct the original network's output.
\begin{equation}
    f\!\left(x \,\big|\, \{W'^{l}(x, r)\}_{l=1}^{L}\right)
    \approx
    f\!\left(x \,\big|\, \{W^{l}\}_{l=1}^{L}\right).
\end{equation}
This is done through both a layer-wise and a global reconstruction loss. The layer-wise version is necessary to provide a stronger training signal as randomly masking all components at the same time can be too noisy to learn from.

\begin{equation}
\begin{aligned}
    \mathcal{L}_{\text{stochastic-recon}}
      &= \frac{1}{S} \sum_{s=1}^{S}
         D\!\left(f\bigl(x \mid W'(x, r^{s})\bigr),\; f(x \mid W)\right), \\
    \mathcal{L}_{\text{stochastic-recon-layer}}
      &= \frac{1}{LS} \sum_{l=1}^{L} \sum_{s=1}^{S}
         D\!\left(f\bigl(x \mid W^{1}, \dots, W'^{\,l}(x, r^{(s)}), \dots, W^{L}\bigr),\;
                  f(x \mid W)\right).
\end{aligned}
\end{equation}
where $D$ is a task-specific divergence measure, such as MSE or KL divergence.

Lastly, to enforce minimality, the importance function is
trained to activate only the subcomponents needed for reconstruction.
The loss balances reconstruction accuracy against the number of active
subcomponents, creating a tradeoff between faithfulness and
minimality. The goal is to activate only the subcomponents required
to reproduce the target model's output while leaving the rest inactive.
\begin{equation}
    \mathcal{L}_{\text{minimality}}
      = \sum_{l=1}^{L} \sum_{c=1}^{C} \bigl(g^{l}_{c}(x)\bigr)^{p},
      \qquad p > 0.
\end{equation}
\label{spd_minimality}

The full SPD objective is a weighted sum of these terms:
\begin{equation}
    \mathcal{L}_{\text{SPD}}
      = \mathcal{L}_{\text{faithfulness}}
      + \beta_{1}\, \mathcal{L}_{\text{stochastic-recon}}
      + \beta_{2}\, \mathcal{L}_{\text{stochastic-recon-layer}}
      + \beta_{3}\, \mathcal{L}_{\text{minimality}}.
\end{equation}

\subsection{Irreducibility and the natural unit of decomposition}
\label{sec:irreducibility}
To appropriately evaluate the performance of a multi-dimensional feature decomposition method,
it's necessary to have a clear definition of what the ground-truth decomposition is. The original
superposition hypothesis states that the hidden states can be represented as a linear combination of sparse one-dimensional features. \cite{elhage2022toymodelssuperposition,engels2025languagemodelfeaturesonedimensionally}

\textbf{Definition:} The hidden state 
$x_{i,l}$ can be written as:
\begin{equation}
    x_{i,l} = \sum_i v_i \, f_i(t)
\end{equation}
where $v_{i} \in \mathbb{R}^d$ are pairwise $\delta$-orthogonal directions \footnote{should i say what delta is or is it clear that its small?} and $f_{i}(t) \in \mathbb{R}$ are sparse scalar activations, with $f_{i}(t)=0$ outside the domain of feature $i$.

Recent work has shown, that this may be insufficiently strict \autocite{smith2024strong,engels2025languagemodelfeaturesonedimensionally}, as it does not capture the structure of multi-dimensional features.
In particular, transformer models are shown to
organise cyclical concepts in low-dimensional subspaces
that do not decompose into meaningful linear components, but are instead manipulated jointly \autocite{engels2025languagemodelfeaturesonedimensionally}.

To accomodate these features, the same paper proposes a correction to the superposition hypothesis,
alongside a formal definition of irreducibility that defines,
when a feature is in its atomic form and when it can be decomposed into smaller
units without loss of information.

\textbf{Definition:} A feature $f: \mathbb{R}^{i} \to \mathbb{R}^{d_f}$ is
truly multi-dimensional, if it cannot be written as a sum of two independent features
or as a the sum of two non-co-occurring features. More formally, a feature is
reducible into features
$a$ and $b$ if there exists an affine transformation
\begin{equation}
    f \mapsto Rf + c \equiv \binom{a}{b}
\end{equation}
for some orthonormal $d_{f} \times d_{f}$ matrix $R$, and additive constant c, such that
the joint distribution $p(a, b)$ satisfies one of two conditions:
\renewcommand\labelitemi{\tiny$\bullet$}
\begin{itemize}
    \setlength\itemsep{0em}
    \item \textbf{$p$ is separable}: $p(a, b)= p(a) \, p(b)$
    \item \textbf{$p$ is a mixture of two disjoint distributions}: 
    $p(a, b) = w \, p(a) \, \delta(b) + (1 - w) \, p'(a,b | b \neq 0)$,

\end{itemize}
Here $a$ and $b$ are the first $k$ and remaining $d_f - k$ components of $Rf + c$,
$p$ is conditioned on inputs where $f$ is active, $w \in (0, 1)$ and $\delta$ is the Dirac delta \footnote{can i get away with just writing $p(a|b=0)$}.
In the mixture, the two distributions have disjoint support, with one distribution having no mass where $b$ is active, and the other having no mass where $b$ is zero.

Under this decomposition, the superposition hypothesis takes the form:


\textbf{Definition:} Every hidden state $x_{i,l}(t)$ can be written as a sum of feature contributions
\begin{equation}
    x_{i,l}(t) = \sum_i V_i \, f_i(t)
\end{equation}
where each $f_i(t) \in \mathbb{R}^{d_{f_i}}$ is an irreducible
feature with internal dimensionality $d_{f_i} \geq 1$, each
$V_i \in \mathbb{R}^{d \times d_{f_i}}$ and $\| V_i^\top V_j \| < \delta$ for $i \neq j$.


Secondly, and more importantly for SPD, if the multidimensional
superposition hypothesis holds, some features f may require multiple
rank-one subcomponents. It is not trivial that an irreducible function
f (e.g. as shown in Section \ref{fig:tms-paired-comparison}) arises from sampling
two dependent and non-overlapping features that can still be compressed 
into a single rank-one component if the model learns to group them along a shared direction.

However, when the model learns genuinely multi-dimensional features,
these cannot in general be represented by a rank-one subcomponent.
Instead, they occupy structured higher-dimensional subspaces,
implying either the need for higher-rank representations or post hoc
clustering of the underlying rank-one components.

\subsection{Rank-$k$ subcomponents and relational importance}
\label{sec:rank-k-attention}
In the original SPD paper, the subcomponents are constructed as rank-one matrices, through an outer product of two vectors.
This formulation is however incompatible with the multi-dimensional superposition hypothesis. (Section \ref{sec:irreducibility})
A decomposition into lower-dimensional subcomponents, necessarily forces features to be split across multiple subcomponents, or to be approximated through a single subcomponent at the cost of reconstruction.

To represent multi-dimensional features, the pairs of vectors are replaced with pairs of matrices.
\begin{equation}
W^{l}_{i,j} \approx \sum_{c=1}^{C} \sum_{r=1}^{k} U^{l}_{c,i,r} V^{l}_{c,j,r},
\quad \text{where} \quad U^l \in \mathbb{R}^{C \times d_{\text{out}} \times k}, \, V^l \in \mathbb{R}^{C \times d_{\text{in}} \times k}
\end{equation}
Each subcomponent $U^l_c {V^l_c}^{\top} \in \mathbb{R}^{d_{\text{out}} \times d_{\text{in}}}$
has rank at most $k$, recovering the original SPD definition when $k=1$.
As in the multi-dimensional superposition hypothesis, $k$ is an upper bound on
each subcomponent's representational capacity; the effective rank may be lower,

In SPD, each subcomponent’s importance is computed independently through an MLP followed by a sigmoid nonlinearity,
allowing multiple subcomponents to be active for a given input and thus distribute a
feature across them, even when it is irreducible. Likewise, the minimality loss does not translate to the rank-k setting, where each subcomponent can represent multiple feature directions:
it imposes no penalty for distributing a feature across multiple subcomponents,
nor for a single subcomponent to encode parts of multiple features. (Section \ref{spd_minimality})

To address this, the causal importance function is redefined to maximize the conditional probability, that a given subcomponent $W_{c}^{l}$, is responsible for a given feature, conditioned on the activations of all other subcomponents in the same layer.
\begin{equation}
    g^l_{c}(x) = p\!\left(W^l_c \mid \{h^l_{c'}(x)\}^{C}_{c'=1}\right), \quad h^l_c(x) = {V^l_c}^{\top} \vec{a}^l(x) \in \mathbb{R}^k
\end{equation}
where $\vec{a}^l(x)$ is the activation of layer $l$ and C is the set of subcompoennts in layer $l$.
The distribution is computed with a classic transformer block operating over subcomponents, with a softmax over their activations.
\autocite{vaswani2023attentionneed}

This produces a normalized assignment over subcomponents, $\sum_{c=1}^{C}g^l_c(x) = 1$,
causing the original minimality loss to become degenerate, as each layer contributes a constant value independent of the model state.
Instead, it's replaced
with entropy, to pressure the assignment to concentrate on as few subcomponents as possible. 
\begin{equation}
    \mathcal{L}_{\text{minimality}} = - \frac{1}{|L|} \sum_{l=1}^{L} \sum_{c=1}^{C} g^l_c(x) \log g^l_c(x)
\end{equation}

An unfortunate limitation of this formulation is its computational cost. Computing
relational importance via a transformer over subcomponents scales
quadratically with the number of subcomponents compared to the linear cost of the original independent importance functions.  \footnote{Play with attention approximations}

\section{Results}
\subsection{Overview of experiments}
To evaluate how the proposed changes affect SPD’s performance,
and how these relate to the multi-dimensional superposition hypothesis,
the result section is structured around three experiments \footnote{maybe 4}, each with the purpose of isolating a specific structural property.
For an overview see table \ref{tbl:Experiment_overview}.

The first experiment reuses the Toy model of Superposition (TMS) \autocite{elhage2022toymodelssuperposition}
once sampled with independent features and once with paired features,
where these features are sampled in pairs, creating the simplest case of
statistical dependence. In particular, the model learns to group the paired features along a single direction, allowing
rank 1 subcomponets to capture full features, without having to split them across multiple subcomponents. 


The second experiment also reuses a known experiment from SPD, the Toy model of compressed computation,
where a model must learn to compute more functions, than the number of available directions in a lower dimensional space.
Particularly, this experiment is interesting to explore how softmax and attention affect the decomposition.
Softmax in particular, is best when the ground truth decomposition is meant to pick a singular subcomponent for each feature,
due to its properties[some source], the toy model of compressed computation, does not allow for a one-hot assignment,
as no disentanglement is possible, and to stay faithful to the original model, should learn to distribute mass across multiple subcomponents.


The third experiment generalizes the paired setting to a
k-dimensional case by computing the volume of a simplex
from its vertices. The ground truth decomposition is still
shown to be dependent, but the features can now not be contained in a single rank-one subcomponent, as the geometry of the feature is more complex.
This experiment serves to test how well softmax and sigmoid gating can group feature parts together, when their expressiveness is increased.

% Finally, the fourth experiment trains a model to learn a logical
% expression composed of two disjoint distributions,
% one of which is lower-dimensional. This experiment tests
% the final condition of the irreducibility
%  definition (Section \ref{sec:irreducibility}).
\begin{table*}[h!]
\centering

\renewcommand{\arraystretch}{1.25}
\setlength{\tabcolsep}{5pt}

\begin{tabularx}{\textwidth}{
    >{\raggedright\arraybackslash}p{3.2cm}
    *{4}{>{\centering\arraybackslash}X}
}
\toprule

&
\textbf{{Toy Model of Superposition}}
&
\textbf{{Toy Model of Dependent Superposition}}
&
\textbf{Toy Model of Compressed Computation}
&
\textbf{{Toy Model of Geometry}}
\\

\midrule

% ---------------- Mapping row ----------------
Objective & $x \in \mathbb{R} \mapsto x$ 
          & $x \in \mathbb{R}^d \mapsto x$ 
          & $x \in \mathbb{R} \mapsto x + \mathrm{ReLU}(x)$ 
          & $x \in \mathbb{R}^d \mapsto \mathrm{vol}(x) \in \mathbb{R}^{+}$\\
\midrule

Statistical dependence&
$ \times  $
&
$\checkmark$
&
$ \times  $
&
$\checkmark$
\\
Formal feature dimension
&
$1$
&
$2$
&
$1$
&$n(n+1)$
\\
Component rank required
&
$1$
&
$1$
&
$1$
&
$k$
\\
Ground-truth decomposition
&
One-hot
&
One-hot per pair
&
Distributed
&
One-hot per simplex
 \\

\bottomrule
\end{tabularx}

\vspace{0.5em}

\caption{Overview of the toy models and their properties.
}
\label{tbl:Experiment_overview}

\end{table*}

\subsection{Toy Model of Superposition}
\label{sec:tms-experiments}

The Toy Model of Superposition (TMS) \autocite{elhage2022toymodelssuperposition} is trained to reconstruct sparse feature activations through a lower-dimensional bottleneck that forces the model to learn features in superposition. The architecture is a single-hidden-layer autoencoder with tied weights:
\begin{equation}
    \hat{x} = \mathrm{ReLU}(W^{\top}Wx + b), \qquad W \in \mathbb{R}^{d_{\text{hidden}} \times d_{\text{features}}}, \quad d_{\text{features}} \gg d_{\text{hidden}}.
\end{equation}
TMS is studied in two regimes: one with independent features, reproducing the original SPD setup, and one with paired features that introduces statistical dependence. For both regimes, the rank of each subcomponent is kept at $1$, both because this is the ground-truth structure and to isolate the effect of softmax-and-attention.

To quantify how well the learned subcomponents recover the target model, two metrics are computed across both regimes. The mean max cosine similarity (MMCS) \autocite{cunningham2023takingfeatures} measures directional alignment between each target feature and its best-matching learned subcomponent:
\begin{equation}
\text{MMCS}
=
\frac{1}{d_{\text{features}}}
\sum_{j=1}^{d_{\text{features}}}
\max_{c}
\left(
\frac{
\vec{U}_{:,c} V_{c,j} \cdot W_{:,j}
}{
\left\| \vec{U}_{:,c} V_{c,j} \right\|_2
\left\| W_{:,j} \right\|_2
}
\right).
\end{equation}
where $c \in C$ is the parameter component index and $j \in \{1, \ldots, d_{\text{features}}\}$ is the input feature index. A value of $1$ indicates that every target feature has a perfectly aligned subcomponent.

Apart from directional alignment, the strength of the alignment is also measured, to detect feature shrinkage, a problem apparent in both Sparse Autoencoders and Attribution-Based Parameter Decomposition \autocite{jermyn2024tanhPenaltyDictionaryLearning,wright2024featureSuppressionSAEs,AttributionBasedParameterDecomposition}. The mean $L_{2}$ ratio (ML2R) compares the euclidean norm of the best-aligned subcomponent to the target feature \autocite{stochasticparameterdecomposition}:
\begin{equation}
\text{ML2R}
=
\frac{1}{d_{\text{features}}}
\sum_{j=1}^{d_{\text{features}}}
\frac{
\left\| \vec{U}_{:,\mathrm{mcs}(j)} \vec{V}_{\mathrm{mcs}(j),j} \right\|_2
}{
\left\| W_{:,j} \right\|_2
}
\end{equation}
where $\mathrm{mcs}(j)$ is the index of the most aligned subcomponent for feature $j$. A ratio near $1$ indicates the feature is fully captured by its best-aligned subcomponent.

\subsubsection{Independent features}

Each feature is drawn independently from a uniform distribution over $[0, 1]$. As each feature is one-dimensional and independent of the others, the ground-truth decomposition is a set of $d_{\text{features}}$ rank-one matrices, each non-zero only along the $c$-th column $W_{:,c}$ of the target weights.

Like in the original SPD paper, two TMS models are trained: $\mathrm{TMS}_{5\text{-}2}$ with $d_{\text{features}}=5$ and $d_{\text{hidden}}=2$, and $\mathrm{TMS}_{40\text{-}10}$ with $d_{\text{features}}=40$ and $d_{\text{hidden}}=10$ with an additional identity layer.

\begin{figure}[h!]
    \centering
    \includegraphics[width=\linewidth]{tms_5_2_plot.png}
    \caption{Decomposition of $\mathrm{TMS}_{5\text{-}2}$ using Softmax. The first two panels show the target model's weight columns and the sum of learned subcomponents. Subsequent panels show the individual subcomponents, ordered by decreasing norm. Although $20$ subcomponents are allocated, only the first $5$ carry meaningful norm and contribute to reconstruction, while the remaining $15$ have negligible weight and are effectively pruned by the method. Each active subcomponent recovers primarily one feature direction, with minor leakage across components.}
    \label{fig:tms_5_2}
\end{figure}

SPD achieves near perfect alignment on both models, with MMCS and ML2R approximately equal to $1$. The softmax and attention variant, while achieving near-zero losses, does not manage to perfectly align its components with the target model nor concentrate the entire feature magnitude within a single subcomponent, resulting in a lower ML2R with high variance. For the larger model this skews the geometry further.

Insert a table for this!\footnote{important}

While the softmax variant does not recover the exact geometry, it correctly concentrates the entire magnitude on the right number of subcomponents, with the rest pruned to near-zero (Figure~\ref{fig:tms_5_2}). In terms of causal importance values, the softmax and original SPD method show the same one-hot assignment for each of the five features in $\mathrm{TMS}_{5\text{-}2}$, with some degradation in the larger model where certain subcomponents are causally important for multiple features.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=7cm]{tms_5_2_plot_causal_importances.jpg}
    \caption{Causal importance values for the softmax variant on $\mathrm{TMS}_{5\text{-}2}$, showing a one-hot assignment for each of the five features.}
\end{figure}

\subsubsection{Paired features}
\label{sec:tms-tied-experiment}

In this regime, features are sampled in pairs to create the simplest form of dependence. Features are partitioned into pairs $\{f_{2i-1}, f_{2i}\}_{i=1}^{20}$, with each pair zero with probability $1-p$, and otherwise each pair member drawn independently from $\mathcal{U}(\epsilon, 1)$. The setup uses $d_{\text{features}} = 40$ and $d_{\text{hidden}} = 10$.

Each input is 2-dimensional at construction, but the trained model learns a feature mapping that aligns both pair members along a single direction, with the two features in each pair being manipulated jointly.
(Table~\ref{tab:tms-paired-cosine-similarity}), with within-group cosine similarity averaging $0.999$ and across-group similarity averaging $0.053$. The pair's hidden representation therefore collapses to a 1D subspace. Due to this lower-dimensional mapping, paired features can be captured with a single rank-1 subcomponent, without needing to split them across multiple subcomponents.

\begin{table}[h]
\centering
\begin{tabular}{lccc}
\hline
Group        & Mean    & Std Dev & Range \\
\hline
Within-group & $0.999$ & $0.010$ & $[0.997, 1.000]$ \\
Across-group & $0.053$ & $0.223$ & $[-1.000, 0.038]$ \\
\hline
\end{tabular}
\caption{Cosine similarity between encoder columns $W_{:,i}$ and $W_{:,j}$, partitioned by group membership.}
\label{tab:tms-paired-cosine-similarity}
\end{table}

The ground-truth decomposition is therefore $20$ rank-1 subcomponents, with feature indices $\{2i-1, 2i\}$ assigned to subcomponent $i$,
where the causal importance function should assign all importance to the correct subcomponent for each feature pair, and zero importance to the rest.

In terms of losses, both SPD and Softmax achieve total losses around $10^{-6}$, with no significant differences between the two methods.
In terms of MMCS, both methods achieve near-perfect alignment, with values around $0.999$. 

There needs to be another table here.

However, the softmax variant shows a much lower ML2R with high variance, indicating that it struggles to capture the full magnitude of the features within single subcomponents.
In terms of causal importances, the two methods show very different patterns, despite identical losses and MMCS.
SPD correctly identifies that features should be paired in single subcomponents but does not isolate each pair into its own subcomponent, with several subcomponents carrying importance for multiple feature pairs. The softmax variation correctly assigns each feature pair to a single subcomponent, with the exception of one pair (Figure~\ref{fig:both}).

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.48\linewidth,height=10cm]{spd_40_10_paired.jpg}
    \hfill
    \includegraphics[width=0.48\linewidth,height=10cm]{softmax_40_10_paired.jpg}
    \caption{Left: SPD decomposition of the paired TMS model. Right: Softmax of the paired TMS model. SPD finds the correct structure of pairing features together but does not attribute one feature pair per subcomponent. Softmax achieves the desired importance decomposition, with the exception of one feature pair. SPD was trained with $200$ subcomponents and Softmax with $100$, as these yielded the best results for each method.}
    \label{fig:both}
\end{figure}

\begin{table}[h!]
\centering
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lcccccc}
\toprule
& \multicolumn{2}{c}{$\mathrm{TMS}_{5\text{-}2}$} & \multicolumn{2}{c}{$\mathrm{TMS}_{40\text{-}10}$} & \multicolumn{2}{c}{$\mathrm{TMS}_{40\text{-}10}$ paired} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
& SPD & Softmax & SPD & Softmax & SPD & Softmax \\
\midrule
MMCS  & $1.00$ & $0.985 \pm 0.016$ & $1.00$ & $0.812 \pm 0.105$ & $0.9997$ & $0.9920 \pm 0.003$ \\
ML2R  & $1.00$ & $0.797 \pm 0.205$ & $1.055 \pm 0.045$ & $0.623 \pm 0.231$ & $0.796 \pm 0.434$ & $0.732 \pm 0.350$ \\
\bottomrule
\end{tabular}
\caption{Decomposition quality across both TMS regimes. SPD refers to the original method \autocite{stochasticparameterdecomposition}; Softmax refers to the relational importance function of Section~\ref{sec:rank-k-attention}. If the standard deviation is not reported, it's because its smaller than $0.001$.}
\label{tab:tms_results}
\end{table}


Overall across the three settings, SPD with sigmoid gating produces better results on both MMCS and ML2R, despite the more entangled importance assignments observed in the paired regime (Figure~\ref{fig:both}). The MMCS gap is generally small, but with substantially larger variation in ML2R. 

Referring back to the $\mathrm{TMS}_{5\text{-}2}$ decomposition shown
in Figure~\ref{fig:tms_5_2}, the softmax method appears to recover a
slightly rotated geometry, with residual feature magnitude distributed
across adjacent features. The geometric offset may arise as a compensatory
mechanism for this residual leakage, allowing the combined subcomponents
to maintain accurate reconstruction despite imperfect feature isolation.

\subsection{ Toy Model of Compressed Computation }

The Toy Model of Compressed Computation,
a model that was already studied by SPD \autocite{stochasticparameterdecomposition}, APD \autocite{AttributionBasedParameterDecomposition} and more recently by \autocite{bhagat2025compressed}, is a
toy model trained to compute the elementwise function $y_i = x_i + \mathrm{ReLU}(x_i)$ through a residual MLP with a lower-dimensional hidden layer.

The architecture consists of an embedding matrix $W_E$, an unembedding matrix $W_U = W_E^\top$, and a residual MLP with input projection $W_{\text{in}} \in \mathbb{R}^{50 \times d_{\text{emb}}}$ and output projection $W_{\text{out}} \in \mathbb{R}^{d_{\text{emb}} \times 50}$.
The input vectors are sparse activations sampled from a uniform distribution over $[-1, 1]$, with the model trained to minimize the MSE between the output and the target function. The model's computation can be expressed as:
\[
    y = W_U \big( W_E x + W_{\text{out}}(\mathrm{ReLU}(W_{\text{in}} W_E x)) \big).
\]
For comparability with previous work, the experiments use $d_{\text{features}} = 100$, $d_{\text{MLP}} = 50$, rank $k=1$ matrices, $C = 200$ subcomponents and lastly an embedding dimension $d_{\text{emb}} = 1000$.

\begin{figure}[H]
    \centering
    \includegraphics[width=10cm, height=5cm]{CCmodel.png}
    \caption{Architecture of the Toy Model of Compressed Computation. Picture taken from \autocite{bhagat2025compressed}}
\end{figure}

A naive decomposition of this model would be to let
each of the neurons in the hidden layer compute one of the $100$ ReLU computations.
However this would would perform poorly, on the remaining $50$ neurons. Instead,
to achieve a better reconstruction, the model seems to learn a more efficient representation by assigning
multiple features to the same neuron.

The ground truth for $W_{\text{in}}$ is clear:
each subcomponent should be causally important for one input feature
matching the independent one-dimensional structure of the inputs.

For $W_{\text{out}}$, the ground truth is less clear.
APD \autocite{AttributionBasedParameterDecomposition} initially suspected a monosemantic decomposition where each subcomponent corresponds to one output feature,
to be the most truthful decomposition. SPD \autocite{stochasticparameterdecomposition} on the other hand
reports a polysemantic decomposition in which $50$ subcomponents
each contribute to multiple features while the remaining
are pruned to near-zero norm.


Another paper \autocite{bhagat2025compressed}
supports the interpretation of SPD, showing that 
the model does not implement $100$ separate computations
through superposition but rather that they jointly operate within a shared
$50$-dimensional subspace.


\subsubsection{Sampling effect on loss}
Before analyzing the decomposition, this experiment will also be used to compare the effect of different sampling distributions on the loss
and decomposition quality. Here the two methods SPD and SPD with softmax are compared across two sampling regimes: one-hot sampling, where only one feature is active at a time, and at-least-zero sampling, where each input is independently sampled with probability $p$.

Softmax generally produces lower losses when sampling is done with one-hot inputs rather than sampling every feature independently.
Most noticeably, the Minimality loss, does not converge under at-least-zero sampling.
This is unsurprising, with how the loss is defined to maximize the probability of a feature belonging to a single subcomponent.
However if the input distribution is not one-hot, the input is a noisy and potentially meaningless combination of unrelated features.

Sigmoid-based importance does not exhibit a significant difference between
the two sampling methods, likely since 
each subcomponent's importance is evaluated independently,
and the loss measures the magnitude of subcomponent activation (Section \ref{spd_minimality})
to the correct contribution.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
& \multicolumn{2}{c}{SPD} & \multicolumn{2}{c}{Softmax} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Metric & One-hot & At-least-zero & One-hot & At-least-zero \\
\midrule
Faithfulness & $1.5 \times 10^{-6}$ & $1.4 \times 10^{-6}$ & $3 \times 10^{-5}$ & $5 \times 10^{-5}$ \\
Stochastic reconstruction &$1.8 \times 10^{-4}$ & $1 \times 10^{-5}$ & $9 \times 10^{-5}$ & $1 \times 10^{-4}$ \\
Layerwise stochastic reconstruction & $1 \times 10^{-5}$ & $1 \times 10^{-5}$ & $7 \times 10^{-5}$ & $9 \times 10^{-5}$ \\
Output reconstruction & $3 \times 10^{-6}$ & $4 \times 10^{-6}$ & $8 \times 10^{-5}$ & $1.6 \times 10^{-4}$ \\
Minimality & $3.83$ & $3.95$ & $8 \times 10^{-5}$ & $4.06$ \\
\bottomrule
\end{tabular}
\caption{Reconstruction quality across sampling regimes for the Residual MLP.}
\label{tab:resid_mlp_sampling}
\end{table}

\subsubsection{Causal importance}
The experimentation with Softmax seems to replicate a similar structure to that 
found in APD \autocite{AttributionBasedParameterDecomposition},
where one subcomponent is causally important for one feature,
while the remaining subcomponents have negligible importance for that feature in both layers.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=8cm]{softmax_CC.jpg}
    \caption{Causal importance values for Softmax on the Toy Model of Compressed Computation. Each input feature activates a near one-hot importance pattern across subcomponents. Sampled using one-hot sampling.}
    \label{fig:cc_importance}
\end{figure}

If this reading is taken at face value,
there should only be two possible interpretations, either
each subcomponent learns a monosemantic representation for one feature, and the remaining
subcomponents are unimportant and can be ablated. Alternatively,
the minimality loss is too strong and forces the model to concentrate all importance on a single subcomponent, at the cost of reconstruction
which should be reflected in a high reconstruction error.
Surprisingly, as seen in table \ref{tab:resid_mlp_sampling},
Softmax produces near-zero reconstruction error,
and should therefore have recovered a monosemantic decomposition.

To examine this, the neuron contributions for both the target model and softmax are plotted in figure \ref{fig:cc_contribution}.
The computation of the contribution is computed as in SPD \autocite{stochasticparameterdecomposition}.
\begin{align}
    \text{NeuronContribution}_{\text{model}}(i) &= \left( W_{U[i,:]}W_{out} \right) \odot \left( W_{in}W_{E}[:,i] \right) \\
    \text{NeuronContribution}_{\text{subcomponent}}(i) &= \max_{c \in C} \left[\left( W_{U[i,:]} U_{\text{out}} V_{\text{out}^{T}} \right) \odot \left(U_{\text{in}[:,c]}V^{T}_{in}[c,:] W_{E}[:,i] \right) \right] 
\end{align}
where $W_{E[:,i]}$ and $W_{U[i,:]}$ are the $i$-th column and row of the embedding and unembedding matrices
and $\odot$ denotes elementwise multiplicaiton.

$ W_{U[i,:]}W_{out} $ measures how strongly the neurons contribute to the "reconstruction" of the output,
while $ W_{in}W_{E}[:,i] $ measures how strongly the neurons are activated by the input feature $i$.
If both produce high values, the product will be high, indicating that the neuron contributes strongly to the feature's computation.
Otherwise if either is low, the contribution will be low, indicating that the neuron does not contribute significantly to that feature.

As seen in figure \ref{fig:cc_contribution},
Softmax, like SPD, find subcomponents that contribute to multiple features,
which also replicates the behaviour of the target model.
\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=8cm]{component_contribution_soft_max.jpg}
    \caption{Per-feature contribution of each subcomponent at the neuron level. Subcomponents contribute to multiple features simultaneously, even when their causal importance is concentrated on a single feature.}
    \label{fig:cc_contribution}
\end{figure}

The one-hot importance assignment is therefore not equivalent to a monosemantic
parameter decomposition. One initial reading would be that the subcomponents
represent multiple features in superposition, with the observed interference
arising from near-orthogonal subcomponents. However,this is contradictory to the results found by \autocite{bhagat2025compressed}, which show that the trained $W_{\text{out}}$ operates within a shared $50$-dimensional subspace rather than implementing $100$ separate computations.

If softmax was genuinely encoding $100$ features in superposition,
the $U_c$ and $V_c$ vectors of its $100$ active subcomponents would have to be near-orthogonal.
To examine this, the pairwise cosine similarity between the $U_c$ and $V_c$
vectors of the causally important subcomponents are computed
in figure \ref{fig:CC_cosines}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.48\linewidth,height=7cm]{CC_softmax_cosines.jpg}
    \hfill
    \includegraphics[width=0.48\linewidth,height=7cm]{CC_spd_cosines.jpg}
    \caption{Left: Softmax. Right: SPD. Pairwise cosine similarity between the $U_c$ and $V_c$ vectors of the causally important subcomponents for both layers. Subcomponents are ordered by decreasing norm.}
    \label{fig:CC_cosines}
\end{figure}

For $W_{\text{in}}$, both methods recover near-orthogonal $V$ vectors,
an expected result, as the embedding dimension is large enough
to accommodate all pairwise orthogonal input features.

The $U$ vectors, by contrast, must project into the $50$-dimensional
MLP hidden space and therefore cannot be pairwise orthogonal.
Interestingly, the two methods find wildly different projections in the $U$ space.
Softmax finds two clusters, where the first $80$ subcomponents have large positive cosine similarities with each other and large negative similarities with the remaining $20$, while the final $20$ are positively correlated among themselves.
SPD on the other hand find many subcomponents sharing similar directions, but without the clear two-cluster structure observed in SPD.

For $W_{\text{out}}$, the two methods diverge in their representation. In SPD, the $50$ causally important subcomponents recover orthogonal directions in both $U$ and $V$, while the remaining subcomponents are entangled but carry negligible norm and contribute nothing to the final projection.

The softmax variant, which assigns causal importance to $100$ subcomponents, 
presents a more entangled picture: the first $80$ subcomponents have small pairwise cosine similarities in $V$, while the remaining $20$ collapse onto highly similar directions.
The $U$ vectors on the otherhand, find the same two-cluster pattern as in $W_{\text{in}}$, with the first $80$ subcomponents having small positive cosine similarities with each other and large negative similarities with the remaining $20$,
while the final $20$ practically collapse onto the same direction.

\subsection{Subcomponent Norm Distributions}

As briefly mentioned in TMS, but also observed in the CC model,
softmax produces feature shrinkage, even when it produces the right feature directions.
To quantify if this could be the cause of insufficient norm concentration, the norms of the subcomponents are examined. (Figure \ref{fig:CC_norms})
\begin{figure}[H]
    \centering
    \includegraphics[width=0.48\linewidth,height=5cm]{CC_norms_softmax.jpg}
    \hfill
    \includegraphics[width=0.48\linewidth,height=5cm]{CC_norms_spd.jpg}
    \caption{Left: Softmax. Right: SPD. Norms of the subcomponents, ordered by decreasing norm. Matrix norm is the fribenius norm of the subcomponent matrix $U_c V_c^T$. Subcomponents are ordered by decreasing norm.}
    \label{fig:CC_norms}
\end{figure}

The two methods recover very different norm distributions.
Consistent with the cosine similarity analysis, softmax
separates into two broad clusters: approximately the first 80 subcomponents have
substantially larger norms, while the remaining 120 retain smaller but non-negligible norms in both $W_{in}$ and $W_{out}$.

Notably, the softmax does not drive unused subcomponents to zero norm, but rather to a small but non-negligible value.

SPD, by contrast, concentrates norm almost exclusively in the causally important subcomponents: approximately 100 subcomponents for $W_{\text{in}}$ and 50 for $W_{\text{out}}$. The remaining subcomponents are effectively pruned, with norms close to zero rather than forming the residual low-norm tail observed in the softmax decomposition.

A further observation is that the norm contributions differ systematically between the two layers. In $W_{\text{in}}$, the norm is dominated primarily by the $V$ vectors, whereas in $W_{\text{out}}$ the dominant contribution instead comes from the $U$ vectors. This pattern is consistent across both decomposition methods.

An initial hypothesis for why the softmax formulation fails to produce hard-zero norms was that, unlike the hard-sigmoid gating used in SPD, the softmax distribution cannot produce exact zeros. However, the plot over the causal importance value frequencies rule out this explanation (Figure \ref{fig:CC_causal_importance_hist}).
The learned importance assignments are nearly bimodal, with most 
concentrated at either $0$ or $1$, and only a small fraction occupying
intermediate values. (Figure \ref{fig:CC_causal_importance_hist}) The absence of hard-zero norms therefore seems
not to arise from the gating distribution itself, but rather from how
reconstruction mass is distributed across subcomponents during optimisation.
Empirically, this hypothesis was further weakened by replacing the softmax gating with sparsemax \autocite{peters2019sparsesequencetosequencemodels}, which is capable of producing exact zeros in the gating distribution. Despite this, the resulting norm distributions remained qualitatively identical to those obtained with softmax.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.48\linewidth,height=5cm]{cc_softmax_causal_importance_hist.jpg}
    \hfill
    \includegraphics[width=0.48\linewidth,height=5cm]{cc_softmax_causal_importance_hist2.jpg}
    \caption{Distributions of causal importance values for the softmax decomposition on both layers.}
    \label{fig:CC_causal_importance_hist}
\end{figure}

More generally, this behaviour is likely related to the feature
shrinkage phenomenon previously observed in both TMS and CC.
The faithfulness objective is computed additively as the MSE between
the summed subcomponent reconstruction and the target weight matrix,
making it indifferent to how reconstruction mass is allocated
across subcomponents. Consequently, if the importance regularisation only weakly
penalises low-magnitude subcomponents, optimisation may converge
to solutions in which a small set of dominant subcomponents carry
most of the semantic structure, while a long tail of weakly active
components absorbs residual reconstruction error.


\subsubsection{Noes for geometry}
To quantify this, the effective rank of each pair's submatrix, $W_{:,2i-1:2i} \in \mathbb{R}^{d_{\text{hidden}} \times 2}$ is computed using the entropy-based definition of \autocite{effectiverank}.
\begin{equation}
    r_{\text{eff}}(W_g) = \exp\!\left(-\sum_{k=1}^{Q} p_k \log p_k\right),
    \qquad p_k = \frac{\sigma_k^2}{\sum_{j=1}^{Q} \sigma_j^2}
\end{equation}
where $\sigma_k$ are the singular values of $W_g$ and $Q = |g|$.
The quantity lies in $[1, |g|]$ and equals $1$ when the group is
exactly rank-one. To gauge the required value of $k$ for the rank-$k$ extension, the maximum effective rank across pairs is examined:

\begin{equation}
    k = \max_{i} ,\ r_{\text{eff}}(W_{:,2i-1:2i}),
\end{equation}
In this case the max is essentially 1 across all pairs, suggesting that $k=1$ is sufficient to capture these features without loss of information.
(Table~\ref{tbl:mean-effective-rank}).
\begin{table}[h]
\centering
\begin{tabular}{lcc}
\hline
Mean   & Std Dev & Range \\
\hline
$1.0003$ & $0.01$  & $[1.0, 1.05]$ \\
\hline
\end{tabular}
\caption{Effective rank of paired-feature encoder blocks. Values
near $1$ indicate collapse to a single direction.}
\label{tbl:mean-effective-rank}
\end{table}


Related to the table of experiments, the whole point is that there 
is no ground truth decomposition consisting only of the sides
like the cube example !


\textbf{Remarks for this section}
Intra cluster cosine has a -1. Two features are perfectly alligned to a factor of -1,
can this cause issues with decomposition? Same component could capture both features, if they read from the same input.
However they shouldnt as its distinctly different feature indices.



\subsection{Extending to Rank-k: When Higher-Dimensional Structure Matters}
The previous two experiments suggest that SPD outperforms softmax on one-dimensional features,
albeit with degredation in the paired feature regime, where it finds a faithful reconstruction,
but one that is not interpretable in terms of the groud truth feature structure.

This experiment, will relate to two central parts from the introduction,
first the cube example, where the ground truth decomposition can both be taken,
to be individual points, sides or the whole cube, and in this regard
discuss whether SPD and softmax, find either of these decompositions, when their ranks are increased.
In terms of the decomposition itself, each vertex occupies a single 
feature, and depending on how the decomposition is done,
we should see that under f, either one coordinate point,
one vertex or a simplex should be captured in a signle component.


Secondly we will examine how rank-k experimentation,
and different minimality losses influence, how 
and where routing strength is contained.
More particularly different k values, manages to influence
how many directions are captureable in a given component,
and as such should be able to center all the relevant signal
to given subcomponents.
These different subcomponent structures also
admit vastly different importance values, which should again
impact how the routing of these subcomponents happen.


and perhaps even more interestingly, we will highlight settings,
where SPD fails to find any interpretable decomposition, and how fragile the decomposition is,
to the choice of the minimality coefficient.

To construct a setting where the rank-1 assumption of SPD is insufficient,
we will compute the volume of an n-dimensional simplex,
which is given by the formula:
\begin{equation}
    \text{Volume} = \frac{1}{n!} \left| \det\!\left( \begin{bmatrix} v_2 - v_1 & v_3 - v_1 & \cdots & v_{n+1} - v_1 \end{bmatrix} \right) \right|
\end{equation}
where $v_1, v_2, \ldots, v_{n+1}$ are the vertices of the simplex in $\mathbb{R}^n$.

The simplex is particularly interesting, as none of the input features can be written as a linear combination of the others,
and consequently the features are not aligned to a single direction in the input space.



While the previous experiment suggest that softmax is less faithful than SPD,
it was meant to be an extension to SPD, that would allow it to capture higher-dimensional features.
For this reason, the next experiment is meant to show a setting where the rank-1 assumption of SPD is insufficient, at least in the sense of an interpretability perspective.
The rank-1 assumption itself is not a problem for reconstruction, as the additive nature of the loss functions.
Interestingly, for higher dimensional features, SPD seems to show an importance that is not clearly representative of
the ground truth feature structure. The Cosine similarities and one hot encodings, show 
clear structured signal directions, but this is not reflected in the importance values,
for more than a very slim range of the minimality coefficient [1e-4,5e-4],
where all other values show a very split importance. This range is also very narrow and very misable.

Secondly from a structured point of view, when exactly $1e-4$ is chosen,
a clear clustering of the feature structure is observed,
but in this case is only observeable, as the toy model is designed to disentangle features entirely.

We intentionally do not enforce superposition for this experiment,
or projections into lower dimensional subspaces..?

The rank-k extension is required in settings where feature representations do not collapse to a single direction. This is illustrated by a model trained to compute the volume of an n-dimensional simplex.

In this experiment, consider a model trained to compute the volume of an n-dimensional simplex:
\begin{equation}
    f(x) = W_1 \sigma(W_2 x), \quad W_2 \in \mathbb{R}^{k \cdot n(n+1) \times d_{hidden}}, \quad W_1 \in \mathbb{R}^{d_{hidden} \times k}
\end{equation}
Here, k denotes the number of simplices and n the dimensionality of each simplex, which has n+1 vertices.

As in Section~\ref{sec:tms-tied-experiment}, the k simplices are sampled together, with each group sampled with probability p, and otherwise set to zero.
The volume computation cannot be decomposed into independent features,
nor as a disjoint mixture. [build something here].


In terms of decomposition, we aim to find a decomposition that captures the entire simplex as a single unit,
with all n(n+1) features captured in the same subcomponent. To demonstrate, that this is the correct decomposition,
we compute how much each subcomponent group contributes to the hidden layer, and then how these contribute to the output.
By aggregating the feature columns into groups of n(n+1) features, for the input transformation.
For the output, we simply check how much weight each neuron contributes to the output,
and finally we plot where these two align. In the same manner as compressed computation.


\subsubsection{Plots from original model}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=5cm]{geom_input_features.jpg}
\end{figure}


\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=5cm]{geom_output_features.jpg}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=5cm]{geom_input_output.jpg}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=5cm]{geom_input_output_final.jpg}
\end{figure}


It seems pretty clear, that each input group predominately, contributes to a disjoint
set of hidden neurons, and similarly that these hidden neurons contribute disjointly to the output.

A reasonable interpretation of this, as the model is not forced into superposition,
is that it has learnt to disentangle the features into disjoint subspaces.

Secondly, it does not seem to be the case, that like in CC that the model,
has learnt to jointly operate on all features within a shared subspace.

A reasonably, expected result, is that there exists a decomposition,
where each simplex is captured by a set of subcomponents, that do not overlap with those of other subcomponents.
This same interpretation is also expected to hold for the output layer,
where the disentanglement is equally strong.



here we compute the inner product between the column of the encoder
and the simplex vertices,
so we take 6 columns at a time, compute their output,
and then either take a percentage contribution 
to the hideen layer or a total value.
If they become "one hot" then only one simplex contributes to a given
hidden neuron,
if they cluster in 3 clear groups, then we 
can confidently say that under f these implement a joint feature.

Likewise for layer out we do the same for each output column,
we see how much they contribute.


Secondly the visualization of the contribution of each group of vertices to the hidden layer....




\subsection{Comparison of imporance values}
The importance values for 

\subsubsection{Comparison of importance values across different minimality coefficients}

Mention how losses change too!


\subsubsection{Comparison of importance values across different ranks for fixed minimality.}

mention if it affects losses.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=8cm]{geom_dif_ranks.jpg}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=8cm]{geom_input_output_dif_ranks.jpg}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, height=3.5cm]{geom_input_output_final_dif_ranks.jpg}
\end{figure}














\subsection{Computing}
Ideally after constructing these rank k components,
then it should be feasible 
to fully reconstruct the output of the model, by summing these causally important subcomponents together, without any loss of information.
In this case the rank k component should fully capture it.

We try this only for Layer 1 and layer 2 first,

see if they replicate the layer output.

We do the input, output plot, where we only probe the causally important subcomponents.








\subsection{Toy model Quantization:}
Quantization paper computes the parity of a string mod 2,
it proposes control bit sequence, that decide which subset
of the string that is active and should be computed on,

we can sample this with the mixture


The model will have disjoint support, which is good! each control bit,
will activate a different subset of the string.

How do we define the loss function? MSE? entropy?
MSE over expected value?
probably MSE For tracability.

2 layer mlp
$n_{s}+n_{b}$ bits as input,
output a scalar or control bit output?


We do it in two steps:

First, where only one control bit can be active,
and later where multiple control bits can be active.
We squeeze this as the "exactly one" or "at least zero" setup.

Remember to add an experiment that shows if softmax enforce the orthogonality constraint from engels.


% https://arxiv.org/pdf/2404.17563 - this briefly mentions SVD, might be relevant.
% https://arxiv.org/pdf/2303.13506#page=12&zoom=100,144,844
% https://arxiv.org/pdf/2207.08799
% https://ericjmichaud.com/quanta/ -> Can i bring the scaling laws into the mix somehow?

Use equations from quantization paper and irreducibility paper

\textbf{Analytical tools:}
pca+ svd perhaps?
Effective rank -> Rank of submatrices needed -> But on what? i figure in the first 
layer we can do this tractably, and examine the columns of the subsets and see
what rank their group consists of. Trivially here they shouldn't be seperable,
for the second layer it is nt as obvious

cosine similarities?
MUST REMEMBER TO SHOW THE MOTIVATION FOR WHY SPD
IS NOT SUFFICIENT. THE MOTIVATION  FOR THE RANK-K EXTENSION IS NOT JUST TO CAPTURE MORE COMPLEX STRUCTURES, BUT ALSO TO BETTER RECOVER THE GROUND-TRUTH DECOMPOSITION IN SETTINGS WHERE THE ORIGINAL METHOD FAILS TO DO SO, AS DEMONSTRATED IN THE TMS EXPERIMENT
. ALSO THAT IT DOESNT PRESURE THEM INTO GROUPING.

PCA on the training data, do we see the clustering of featureswe
would expect



\subsubsection{Remark for experiments}
Consider trainng on exactly one active.
\section{Discussion}
The original spd method produces hard zeroes,
something that softmax cannot do. Entmax didnt work either nor did sparsemax,
however non hard zeroes do cause magnitude to leak even if marginal, which may increase with more components..
important to mention that the str
ucture that spd decomposes into is not clearsometimes one hots
ome.

Talk about how its unclear what structure spd wants.
Even rank 1 is not the smallest possible, as we can align
two things to the same direction, but these do not split.

Relate back to the interesting thing that one feature per component
and rank 1 matrices does in fact not mean the smallest possible decomposition,
as we have shown with TMSs and TMS paired, one is in a way strictly larger, but they result in the same thing!


Talk about how softmax if working corretly, is probably better at extracting
relevant features, not how it decomposes of smaller parts.
Here it could be nice to touch on, when there genuinely is a one hot solution contra not,
i.e in a lower dimnsional subspace.


Thesis needs to discuss how many of the scenarios from the irreducibility section are actually tested
-Tms serves as dependent features that cannot be split,
-CC serves as an independnet feature - idk if we can do sometihng with wout in this sense?
- Quantization, needs to somehow engage with the two approximation methods, that irreducibility presents.
    -Here we can then say if we have a mixture or if not
-Most likely conclusion becomes that we only discuss cases, where the ground truth is dependence
not if it manages to split non irreducible features.

Discussion needs to engange with the point of,
should the importance value try
to replicate model behaviour or should it try to find
a more disentangled representaiton like SAE's do?


\section{Acknowledgements}

\section{Other things to consider?}
Minimal descriptive length?
\subsection{That some features are multidimensional}
Think back to SAE, that learn a dictionary of features,
then find hte linear combination of those features,
however that this fails for cyclical features, as
its cheaper to learn a single feature, than
learn a direction and a periodic function.
Now for your experiment, the dream scenario is that
we allowe a multi dimensional component,
and that this can learn the cyclical feature, i.e a weekly cycle or modular arithmatic
either do this or extend TMS to be multi-dimensional,
show if it works better with ur gnn or not,
likewise we need to show that through individual masking of features,
we can teach a component to kill one of its features,
if it is better explained by a rank 1 component than rank 2.


\section{References}

\printbibliography[heading=none]


\section{Appendix}
Descriptive length thing?
+ approximation of attention experiment



\section{Notes}
Putting the minimality lsos entropy higher, encourages diagonals?
Instead of capturing two features in the same component.

Increasing minimality loss encourages feature grouping.

SAE: learn to decompose activations, i.e the output of a layer,
Transcoders: learn to approximate the input-output function of a layer



\[
\int p(a,b)\,da\,db
= w \int p_A(a)\,da \int \delta(b)\,db
+ (1-w)\int\!\!\int p_{AB}(a,b)\,da\,db
= w + (1-w)
= 1
\]

to read:
Terence Tao, \emph{An Introduction to Measure Theory}:
Section 1.1 Prologue: The problem of measure --- sets up why measure theory exists and why "lower-dimensional sets have measure zero" is the central idea. This alone might give you the intuition you need.
Section 1.2 Lebesgue measure --- gives you "measure zero," "Lebesgue measure on $\mathbb{R}^n$," and the fact that lines/planes/etc. have measure zero in higher-dimensional ambient spaces. This is the technical core for your question.
Section 2.3 Probability spaces --- short section that frames everything in probability language, which is what you actually want.


\subsubsection{Hard maybes}

\subsubsection{ Per-dimension ablation and implicit rank selection (Hypothesis: ask lukas)}
Following the extension to rank-$k$ subcomponents, and the alternative formulation of the GNAN,
the output $g^{l}_{c} \in [0, 1]^{k}$ is a per feature importance value, and thus the masking can be applied independently to each feature.
The intuition behind this is that if a mechanism captures a 1 dimensional feature, it would only need of the $k$ dimensions to be active.
\\
Likewise if this actually ends up working, k can be considered an upper bound or a so called "capacity" of a mechanism,
it also follows nicely with the descriptive length principle found in the appendix of APD.

To determine the effective rank of each subcomponent, the stochastic masking becomes:
\begin{equation}
    [m_{c}^l(x,r)]_{j} = [g_{c}^l(x)]_{j} + (1-[g_{c}^l(x)]_{j}) r_{c,j}^l, \qquad r_{c,j}^l \sim U(0, 1)
\end{equation}
for each j in $1,\dots,k$
\\
Notably this treats each feature independently, which may not be desirable,
as such we could modify it ever so slightly:

\begin{equation}
        [m_{c}^l(x,r)]_{j} = [g_{c}^l(x)][g_{c}^l(x)]_{j} + (1-[g_{c}^l(x)][g_{c}^l(x)]_{j}) r_{c,j}^l, \qquad r_{c,j}^l \sim U(0, 1)
\end{equation}
The signal becomes an importance of two factors,
if the component is important and both features are important, it remains active,
if only one feature is important, the other will be pushed to zero

The masked weight matrix becomes:
\begin{equation}
    W^{\prime l}(x, r) = \sum_{c=1}^{C} 
    U_{c}^{l} \, \mathrm{diag}\!\left(
    \mathbf{m}_{c}^{l}(x, r)\right) V_{c}^{l\top}
\end{equation}
Where the diagonal matrix is the per-feature mask for subcomponent $c$.

The importance minimality loss penalises all $k$ dimensions of 
all subcomponents toward zero:
\begin{equation}
    \mathcal{L}_{\text{import}} = \sum_{l=1}^{L} \sum_{c=1}^{C} 
    \sum_{j=1}^{k} \left|[g_{c}^{l}(x)]_{j}\right|^{p}
\end{equation}
This follows the same logic by which SPD selects the number of 
active subcomponents: the minimality loss drives unused components 
toward zero, and only those that are genuinely required to 
reconstruct the target model's output survive.

This makes $k$ a soft upper bound rather than a sensitive 
hyperparameter. In practice, $k$ can be set conservatively large 
and the effective rank of each subcomponent is learned from the 
data.


\section{To add:}
https://arxiv.org/pdf/2303.13506 quantization, multidimensional feature crossover
We use the method from the irreducibility paper to quantize how well it is seperable,
mixture like.
Rewrite irreducibility to mkae it clear that it is the function


The method is quite complex,
we practically look for the input space to see where a feature comes from and its dimension
but when we look to decompose a layer, we look at f, the projection of the input space to see 
its rank, but we look for the values that the input space get mapped to.
So in other words, the f decides how many subcomponents we need,
but the f decides how many features are inside of it?

quantization and spd clustering

why softmax + attention instead of only softmax + minimality.

Somewhere add this whole thing of importance maybe being
an interpretability trap? Or that it should be seen as a caution thing,
it will predict what is responsible, not the model alignment.



The thing that makes multifeature hypothesis perfect

Multifeature hypothesis says that there exists a transformation f,
to a lower dimensional space, in this case any W linear map,

that can be be paired with some new $V_{i}$ that describes the hidden state,
in this case it's the SPD components!!!!
These perfectly make this up
and pair this wiht quantization and we are golden.

Remark rewrite TMS paired into TMS, they should be the same section.Rme
Remember V is the
 input reading so if w


 In general add how each experiment relates to the multi dimensional feature hypothesis decomposition.
 


 
\subsubsection{SVD reformulation, capacity maybe per feature ablation TBD}
\label{sec:svd_reformulation}
+ add read write streams, theyre a general thing
-> perhaps issues with diffusion and recurrent 
models where this stream is not as clear. https://arxiv.org/pdf/2504.00194
+ This section must explain delta orthogonality,
visualization of Read, write matrices, perhaps nudge the SVD view.
Most importantly should address if models learn lower dimensional representations
or if its another k sensitivity as in APD.



The competition pressure introduced by the softmax has a
theoretical motivation under the multi-dimensional superposition
hypothesis. The hypothesis predicts that different irreducible
features occupy approximately orthogonal projection subspaces;
the softmax-and-attention mechanism implements a training pressure
toward this orthogonality at the level of SPD's atoms. Atoms whose
$V$ matrices share input directions cannot be cleanly distinguished
on inputs that activate those directions, and the softmax
penalises ambiguous allocations. The optimiser's response is to
push the atoms' $V$ matrices toward disjoint subspaces, recovering
the orthogonality structure the hypothesis posits.

Remember to add an experiment that shows if softmax enforce the orthogonality constraint from engels.



\section{Lukas questions}
- Show him CC,
- Show him geom -> ideas are that i want to both show how "unimportant" the importance values are, for the final decomposition. no readout is still faithful
- Ask about irreducibility -> i define mixtures and so fourth, but its not really used anywhere. Is that okay? It's really hard to assess if it will be one or not.
    - It iwll be in my discussion that we don't examine the mixture case directly. "The independence" is technically between input pairs, and within pair is dependent.
- Ask about formalities, should i do a quey over how ive done litterature search, should my appendix contain my hyperparameter strategy, or can i just link wandb reports, with hyperlinks?
- Do you want the github repo attached to my handin?
- I will exceed the page limit if i use this many images, but thats ok right?

-wants code

-wandb + hyperparameters in appendix.

\end{document}