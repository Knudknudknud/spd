A,B plot
display the norm instead of the direction, as we need to concatenate


thigns to understand:

The forward batched version in the gnan

I changed the dimension in multiple places in the gnan




Four files, here's every change:
run_spd.py — two changes
Change 1: Add rank_k and pass it to ComponentModel:
python    rank_k = 2

    model = ComponentModel(
        ...
        k=rank_k,       # ← add this line
    )
Change 2: Set GNAN in_channels to rank_k:
python    gnan = TensorGNAN(
        in_channels=rank_k,   # ← was 1
        ...
    )
components.py — LinearComponent changes
Change 1: Add k parameter to __init__:
python    def __init__(self, d_in: int, d_out: int, C: int, bias: Tensor | None, k: int = 1):
Store it: self.k = k
Change 2: A and B get extra dimension:
python        self.A = nn.Parameter(torch.empty(d_in, C, k))    # was (d_in, C)
        self.B = nn.Parameter(torch.empty(C, k, d_out))    # was (C, d_out)
Change 3: weight property:
python        return einops.einsum(self.A, self.B, "d_in C k, C k d_out -> d_out d_in")
        # was: "d_in C, C d_out -> d_out d_in"
Change 4: Forward — projection:
python        component_acts = einops.einsum(x, self.A, "... d_in, d_in C k -> ... C k")
        # was: "... d_in, d_in C -> ... C"
Change 5: Forward — masking:
python        if self.mask is not None:
            component_acts = component_acts * self.mask.unsqueeze(-1)
            # was: component_acts *= self.mask
unsqueeze(-1) broadcasts mask from (batch, C) to (batch, C, 1) so it multiplies all k directions equally.
Change 6: Forward — output projection:
python        out = einops.einsum(component_acts, self.B, "... C k, C k d_out -> ... d_out")
        # was: "... C, C d_out -> ... d_out"
component_model.py — three changes
Change 1: Add k to ComponentModel.__init__:
python    def __init__(self, ..., k: int = 1):
        self.k = k
        self.components = self.create_target_components(..., k=k)
Change 2: Pass k through create_target_components to LinearComponent:
python    def create_target_components(self, ..., k: int = 1):
        ...
        components[...] = LinearComponent(d_in=d_in, d_out=d_out, C=C, bias=module.bias, k=k)
Change 3: init_As_and_Bs_ handles the extra dimension:
python        if A.dim() == 3:
            # rank-k
            A.data[:] = A.data / A.data.norm(dim=0, keepdim=True)
            B.data[:] = B.data / B.data.norm(dim=-1, keepdim=True)
            C_norms = einops.einsum(A, B, target_weight, "d_in C k, C k d_out, d_out d_in -> C")
            B.data[:] = B.data * C_norms.unsqueeze(-1).unsqueeze(-1)
        else:
            # rank-1, original code unchanged
component_utils.py — calc_causal_importances
Change 1: Track two things per layer instead of one:
python    all_gate_outputs = {}    # (batch, C) — scalar per component, for gating
    all_gate_feats = {}      # (batch, C, k) — k features per component, for GNAN
Change 2: Projection handles rank-k:
python        A = As[param_name]
        if A.dim() == 3:
            # rank-k: project to (batch, C, k), then norm to (batch, C)
            component_act_k = einops.einsum(acts, A, "... d_in, d_in C k -> ... C k")
            component_act = component_act_k.norm(dim=-1)     # (batch, C)
        else:
            # rank-1: project to (batch, C), unsqueeze for consistency
            component_act = einops.einsum(acts, A, "... d_in, d_in C -> ... C")
            component_act_k = component_act.unsqueeze(-1)    # (batch, C, 1)
The norm collapses k dimensions into one scalar — "how active is this component overall." The k-vector is kept separately for the GNAN.
Change 3: Store both:
python        all_gate_outputs[param_name] = gate_input    # (batch, C) — for gating
        all_gate_feats[param_name] = gate_feats      # (batch, C, k) — for GNAN
Change 4: Build GNAN features with k dims:
python        per_layer_feats = [all_gate_feats[n] for n in pre_weight_acts]
        all_feats = torch.cat(per_layer_feats, dim=-2)   # (batch, total_nodes, k)
        # was: torch.cat(..., dim=-1) giving (batch, total_nodes)
dim=-2 concatenates along the component axis while keeping k intact.
Change 5: Call forward_batched with k features:
python        gnn_out = gnn.forward_batched(
            x_batch=all_feats,          # (batch, total_nodes, k)
            dist_batch=node_distances,
        ).squeeze(-1)                   # (batch, total_nodes)
The GNAN now has k fs MLPs — one per feature dimension. Each processes one of the k activation values. After the matmul and sum over features, the output is still one scalar per node.
Residual and leaky_relu: Unchanged. all_gate_outputs is still (batch, C), layer_out is still (batch, C), addition works the same.






The problem is specific to when C is much larger than the number of features. The GNAN's positive offset prevents dead components from dying. Try TMS with C=40 (matching the feature count) — the GNAN shouldn't need to kill anything, and it might work like it did on ResidualMLP.

