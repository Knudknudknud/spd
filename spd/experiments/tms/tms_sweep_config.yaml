program: spd/experiments/tms/tms_decomposition.py
method: grid
metric:
  name: final_closeness
  goal: minimize
parameters:
  seed:
    values: [0]
  importance_minimality_coeff:
    values: [0.01, 0.05, 0.1, 0.02]
command:
- ${env}
- ${interpreter}
- ${program}
- spd/experiments/tms/tms_config.yaml
